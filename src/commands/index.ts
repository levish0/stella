import * as ping from './ping';
import * as set_message from './set_message';
import * as ticket from './ticket';
import * as close from './close';

export const commands = {
  ping,
  set_message,
  ticket,
  close
};
